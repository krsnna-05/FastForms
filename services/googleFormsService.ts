import getGoogleFormsProvider from "@/providers/googleForms";

class googleFormsService {
  private forms;

  constructor() {
    this.forms = getGoogleFormsProvider();
  }

  const;
}

export default new googleFormsService();
